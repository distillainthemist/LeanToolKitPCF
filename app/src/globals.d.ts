// Build-time constants injected by vite.config.ts `define`.

/** The build's own identity: git describe at build time ("v0.41.2" on
 *  a release tag, "v0.41.2-3-gabc1234" on a dev tree, "dev" without
 *  git). Issue reports carry it (issues plan I1). */
declare const __APP_VERSION__: string;

<?xml version="1.0" encoding="utf-8" ?>
<manifest>
  <control namespace="BenOBrien"
           constructor="BoardGrid"
           version="0.11.6"
           display-name-key="Board Grid"
           description-key="LeanToolKit board grid — the master-leanboard tile wall. Renders each card snapshot INLINE (WebKit-safe, so SVG tiles stay small), in a configurable grid. Read mode: tap a tile to open its card. Edit mode: tap a tile to configure it, tap an empty slot to add a card, drag tiles to swap positions."
           control-type="standard">

    <property name="tilesJSON"
              display-name-key="Tiles (JSON)"
              description-key="The board slots joined to their card data: [{pos, cardId, cardType, title, svg, w, h, barColor, nav}]. svg is raw svgExport markup (rendered inline) or a data image URI (rendered as an image); w/h are optional column/row spans (default 1); barColor fills the tile title chip (associate related cards); nav is the meeting navigation order, distinct from pos. Tiles without a snapshot show a typed placeholder."
              of-type="Multiple"
              usage="input"
              required="false" />

    <property name="gridSize"
              display-name-key="Columns"
              description-key="Number of columns, 1 to 6. Rows are never specified — they grow to fit the tiles, and edit mode keeps a spare blank row at the bottom for adding cards. Empty = an automatic near-square column count. Legacy CxR values still parse (the column count is used)."
              of-type="SingleLine.Text"
              usage="input"
              required="false" />

    <property name="columnTitles"
              display-name-key="Column Titles"
              description-key="Optional headers rendered above the board columns: a JSON array of strings or a comma-separated list, one per column (e.g. Safety, Quality, Delivery). Empty entries leave that column unheaded; empty input hides the header row in read mode. In edit mode the headings become editable fields and changes are emitted in Layout."
              of-type="Multiple"
              usage="input"
              required="false" />

    <property name="editMode"
              display-name-key="Edit Mode"
              description-key="Layout editing: tap a tile to configure it (action configure), tap an empty cell to add a card (action add), drag a tile onto another cell to move or swap it, drag the corner handle to stretch it across cells (both emit Layout). Off = read mode, where tapping a tile emits action open for navigation."
              of-type="TwoOptions"
              usage="input"
              required="false"
              default-value="false" />

    <property name="selectedSlotJSON"
              display-name-key="Selected Slot (JSON)"
              description-key="The tapped slot: {action: open | configure | add, pos, cardId, cardType, title, selectedAt}. open = navigate to the card (read mode); configure = open the composer for this slot; add = open the composer for the empty position. selectedAt changes on every tap so OnChange always fires."
              of-type="Multiple"
              usage="output" />

    <property name="layoutJSON"
              display-name-key="Layout (JSON)"
              description-key="Emitted after a drag moves or resizes a tile, a nav order is edited, or a column heading is edited: {movedAt, slots:[{cardId, pos, w, h, nav}], columnTitles}. Persist the positions, spans, navigation order and column headings into the board manifest in OnChange."
              of-type="Multiple"
              usage="output" />

    <property name="settingsJSON"
              display-name-key="Settings (JSON)"
              description-key="Consolidated configuration: {title, prompts, readOnly, theme:{background,foreground,accent,titlebar,legend,font}, config:{tilesJSON, gridSize, columnTitles, editMode}}. Settings are the base; a non-empty discrete text input overrides its key (boolean inputs merge with OR)."
              of-type="Multiple"
              usage="input"
              required="false" />

    <!-- Self-describing card chrome -->
    <property name="cardTitle"
              display-name-key="Card Title"
              description-key="Optional title bar (e.g. the board name). Leave empty for no chrome at all."
              of-type="SingleLine.Text"
              usage="input"
              required="false" />

    <property name="prompts"
              display-name-key="Prompts"
              description-key="Optional empty-state text shown while the board has no cards: plain text or a JSON array of strings."
              of-type="Multiple"
              usage="input"
              required="false" />

    <!-- Standard styling surface -->
    <property name="backgroundColor"
              display-name-key="Background Colour"
              description-key="Board background colour (any CSS colour or hex). Default white."
              of-type="SingleLine.Text"
              usage="input"
              required="false" />

    <property name="foregroundColor"
              display-name-key="Foreground Colour"
              description-key="Text / line colour. Default rich black."
              of-type="SingleLine.Text"
              usage="input"
              required="false" />

    <property name="accentColor"
              display-name-key="Accent Colour"
              description-key="Accent colour for drop targets and add-card hover. Default rich black."
              of-type="SingleLine.Text"
              usage="input"
              required="false" />

    <property name="titleBarColor"
              display-name-key="Title Strip Colour"
              description-key="Background fill for just the title bar, distinct from the card background - use one colour across related cards to visually associate them on a board. Empty = no strip fill."
              of-type="SingleLine.Text"
              usage="input"
              required="false" />

    <property name="fontFamily"
              display-name-key="Font"
              description-key="Font family (or comma-separated font stack) for the chrome text."
              of-type="SingleLine.Text"
              usage="input"
              required="false" />

    <property name="readOnly"
              display-name-key="Read Only"
              description-key="When true, the grid is display-only: no taps, no editing (wallboard mode)."
              of-type="TwoOptions"
              usage="input"
              required="false"
              default-value="false" />

    <!-- No snapshot outputs: a board-of-boards image would reintroduce the
         very foreignObject-in-img problem this control exists to avoid. -->

    <!-- CSS is bundled into the code (see styles.ts) because canvas apps
         sometimes fail to load a separate css resource. -->
    <resources>
      <code path="index.ts" order="1" />
    </resources>

  </control>
</manifest>

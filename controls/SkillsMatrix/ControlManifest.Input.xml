<?xml version="1.0" encoding="utf-8" ?>
<manifest>
  <control namespace="BenOBrien"
           constructor="SkillsMatrix"
           version="0.7.0"
           display-name-key="Skills Matrix"
           description-key="LeanToolKit skills matrix — team members down the side, skills across the top, quarter-filled discs for proficiency (1 Learning … 4 Can teach). Skills carry a target level; cells below target get a gap ring and headers show coverage. Actions per person."
           control-type="standard">

    <!-- Standard LeanToolKit data surface -->
    <property name="inputJSON"
              display-name-key="Input JSON"
              description-key="Card document to preload: {schema, meta, data:{categories:[{id,name,skills:[{id,name,target}]}], levels:{skillId:{personId:1-4}}}}. Categories, skills and targets are editable + drag-reorderable in-card; the people (columns) come from People (JSON)."
              of-type="Multiple"
              usage="input"
              required="false" />

    <property name="settingsJSON"
              display-name-key="Settings (JSON)"
              description-key="Consolidated configuration: {title, prompts, readOnly, theme:{background,foreground,accent,legend,font}, config:{...card-specific keys named after the discrete inputs...}}. Settings are the base; a non-empty discrete text input overrides its key (enum/boolean inputs: the settings key wins when present)."
              of-type="Multiple"
              usage="input"
              required="false" />

    <property name="resetTrigger"
              display-name-key="Reset Trigger"
              description-key="Reloads the control from Input JSON + Actions Input whenever this value CHANGES (e.g. Set(varReset, Text(Now())))."
              of-type="SingleLine.Text"
              usage="input"
              required="false" />

    <property name="outputJSON"
              display-name-key="Output JSON"
              description-key="The edited card document (no actions — see Actions Output). Read in OnChange and Patch to Dataverse."
              of-type="Multiple"
              usage="output" />

    <property name="actionsInputJSON"
              display-name-key="Actions Input (JSON)"
              description-key="Actions for this card instance, from the central actions table: JSON(Filter(Actions, InstanceId = ...)). Canonical action array."
              of-type="Multiple"
              usage="input"
              required="false" />

    <property name="actionsOutputJSON"
              display-name-key="Actions Output (JSON)"
              description-key="The full current action set, each stamped with Instance Id. Upsert by action id into the central actions table in OnChange. Actions are never hard-deleted — deleting a person cancels their actions."
              of-type="Multiple"
              usage="output" />

    <property name="instanceId"
              display-name-key="Instance Id"
              description-key="Identity of this card instance (e.g. the Dataverse row GUID it is saved against). Stamped on every emitted action."
              of-type="SingleLine.Text"
              usage="input"
              required="false" />

    <property name="peopleJSON"
              display-name-key="People (JSON)"
              description-key="The team: [{whoId, who}]. These are the matrix COLUMNS and also the pool for action assignment. Levels are keyed by whoId."
              of-type="Multiple"
              usage="input"
              required="false" />

    <!-- Self-describing card chrome -->
    <property name="cardTitle"
              display-name-key="Card Title"
              description-key="Optional title bar. Leave empty for no chrome at all."
              of-type="SingleLine.Text"
              usage="input"
              required="false" />

    <property name="prompts"
              display-name-key="Prompts"
              description-key="Optional coaching prompts: plain text, or JSON array of strings / {field, hint} objects. Shown as empty-state text, an info popover, and field hints."
              of-type="Multiple"
              usage="input"
              required="false" />

    <!-- Standard styling surface -->
    <property name="backgroundColor"
              display-name-key="Background Colour"
              description-key="Background colour (any CSS colour or hex). Default white."
              of-type="SingleLine.Text"
              usage="input"
              required="false" />

    <property name="foregroundColor"
              display-name-key="Foreground Colour"
              description-key="Text / line colour. Default rich black."
              of-type="SingleLine.Text"
              usage="input"
              required="false" />

    <property name="accentColor"
              display-name-key="Accent Colour"
              description-key="Accent colour for highlights and badges. Default rich black."
              of-type="SingleLine.Text"
              usage="input"
              required="false" />

    <property name="titleBarColor"
              display-name-key="Title Strip Colour"
              description-key="Background fill for just the title bar, distinct from the card background - use one colour across related cards to visually associate them on a board. Empty = no strip fill."
              of-type="SingleLine.Text"
              usage="input"
              required="false" />

    <property name="legendColors"
              display-name-key="Legend Colours"
              description-key="JSON array or CSV: [unused, disc fill, gap ring] colours. Defaults: disc fill green, gap ring red."
              of-type="SingleLine.Text"
              usage="input"
              required="false" />

    <property name="fontFamily"
              display-name-key="Font"
              description-key="Font family (or comma-separated font stack) for all text."
              of-type="SingleLine.Text"
              usage="input"
              required="false" />

    <property name="readOnly"
              display-name-key="Read Only"
              description-key="When true, disables all editing (display mode)."
              of-type="TwoOptions"
              usage="input"
              required="false"
              default-value="false" />

    <property name="disableActions"
              display-name-key="Disable Actions"
              description-key="When true, hides the add / raise-action affordances so no new actions can be captured on this card. Existing actions stay visible and can still be completed, commented and edited."
              of-type="TwoOptions"
              usage="input"
              required="false"
              default-value="false" />

    <!-- PNG snapshot of the card as a data URI, refreshed after each change. -->
    <property name="pngExport"
              display-name-key="PNG Export (data URI)"
              description-key="Read-only PNG snapshot as a data URI, for an Image control or Power Automate."
              of-type="Multiple"
              usage="output" />

    <!-- SVG snapshot of the card, refreshed alongside the PNG. Vector and
         small — ideal for board tile images (verify Safari renders it in an
         Image control; fall back to pngExport if not). -->
    <property name="svgExport"
              display-name-key="SVG Export (markup)"
              description-key="Read-only SVG snapshot markup, refreshed after each change. Use as an Image control source via &quot;data:image/svg+xml;utf8, &quot; &amp; EncodeUrl(Self.Output) or store next to the card."
              of-type="Multiple"
              usage="output" />

    <!-- CSS is bundled into the code (see styles.ts) because canvas apps
         sometimes fail to load a separate css resource. -->
    <resources>
      <code path="index.ts" order="1" />
    </resources>

  </control>
</manifest>

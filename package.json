{
  "name": "leantoolkit",
  "version": "0.1.0",
  "description": "LeanToolKit — a suite of lean-board cards with a shared design system and common JSON contracts, shipped as the LeanBoard code app (app/)",
  "license": "MIT",
  "author": "Ben O'Brien",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "typescript": "^4.9.5"
  }
}
